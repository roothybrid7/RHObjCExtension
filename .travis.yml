language: objective-c
xcode_project: RHObjCExt.xcodeproj
xcode_scheme: RHObjCExtLogicTests
before_install:
  - brew update
    #  - brew install xctool >/dev/null
  - gem update --system
  - gem install cocoapods
  - bundle install
  - pod install
  - xcodebuild -showsdks
script: bundle exec rake $ACTION
env:
  matrix:
    - ACTION="run_app_test SDK=iphoneos8.1"
    - ACTION="run_logic_test"

